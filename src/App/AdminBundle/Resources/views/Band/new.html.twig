{% extends 'AppAdminBundle::layout.html.twig' %}

{% block content_title %}Band creation{% endblock %}

{% block content -%}
    <div class="panel-body">
        <ul class="record_actions nav nav-pills">
            <li>
                <a href="{{ path('app_admin_band_index') }}">
                    {{ 'back_to_list'|trans }}
                </a>
            </li>
        </ul>

        {{ form(form) }}
    </div>
{% endblock %}
